roboflow:
  workspace: yoloseg-oxj7u
  project: yolo-seg-okjbk
  version: 11
  format: yolov11
  save_path: datasets
  overwrite: true

training:
  model_name: yolo11m-seg
  epochs: 300
  batch_size: 16
  img_size: 640
  rect: true
  device: '0'
  
  optimizer: AdamW
  lr0: 0.01
  lrf: 0.0001
  momentum: 0.937
  weight_decay: 0.0001
  
  warmup_epochs: 3.0
  warmup_momentum: 0.8
  
  box_gain: 7.5
  cls_gain: 0.5
  dfl_gain: 1.5
  
  patience: 50
  workers: 0
  cache: 'disk'
  
  project_dir: models
  pretrain_dir: models/pretrain
  
  
  amp: true
  deterministic: true
  plots: false
  verbose: false
  seed: 42

prediction:
  predict_dir: models/production
  output_dir: output
  confidence: 0.7
  iou: 0.8

video:
  width: 1280
  height: 720
  fps: 30

upload:
  batch_name: null
  split: train
  tags:
    - auto-upload
  frame_list_file: extracted_frames.txt